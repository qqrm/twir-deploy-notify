name: TWIR Dev summary

on:
  workflow_dispatch:

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: write
  actions: read

jobs:
  notify:
    name: TWIR dev summary delivery
    uses: ./.github/workflows/common-delivery.yml
    secrets: inherit
    with:
      environment: dev
      send_main: false
      send_dev: true
      rust_log: debug
